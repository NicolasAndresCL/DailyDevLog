# DailyDevLog — dependencias por perfil (ver carpeta requirements/).
#
#   API / producción:   pip install -r requirements/base.txt
#   Desarrollo + tests:  pip install -r requirements/dev.txt
#   GUI de escritorio:   pip install -r requirements/desktop.txt
#   Documentación:       pip install -r requirements/docs.txt
#
# Por defecto se instala el backend (base). Antes este archivo era un `pip freeze`
# plano de 100 paquetes que arrastraba stacks de otros proyectos (SQLAlchemy/alembic,
# altair/plotly/kaleido, mkdocs, etc.); esos se podaron al separar por perfiles.
-r requirements/base.txt
